#include <iostream>
#include <vector>
#include <algorithm>


using std::vector;

bool binary_search(vector<int>& result, vector<int>& arr1, vector<int>& arr2)
{
	__m128i arr1 = _mm_load_si128()
}

int main()
{
	__declspec(align(32)) vector<int> arr1 = { 1, 2, 3, 4, 5 };
	__declspec(align(32)) vector<int> arr2 = { 1, 2, 3, 4, 5 };
	__declspec(align(32)) vector<int> result;

	return 0;
}